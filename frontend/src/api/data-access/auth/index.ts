// function to get the current logged in user in server side

// // implementation steps
// get the stored session-token cookie
// decode the JWT token and retrive user info such as userId, email, and accountId
export const getUser = () => {
  try {
  } catch (error) {}
};
